name: GitHub Pages workflow (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "GitHub Pages workflow disabled. Use Vercel for deployment."
